<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="horizontal"
    android:layout_marginHorizontal="@dimen/recycler_horizontal_space"

    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/header_title_text"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textColor="@color/text_primary"
        android:textSize="@dimen/header_text"
        tools:text="@string/header_second"/>

    <TextView
        android:id="@+id/header_action_text"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:gravity="center_vertical"
        android:paddingHorizontal="@dimen/header_action_space"
        android:background="?attr/selectableItemBackground"
        android:textColor="@color/text_accent"
        android:textSize="@dimen/header_action_text"
        tools:text="@string/action_second"/>

</LinearLayout>